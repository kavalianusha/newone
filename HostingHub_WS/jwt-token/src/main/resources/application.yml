server:
  port: 8070

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/keerthi
    username: root
    password: 1309
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
